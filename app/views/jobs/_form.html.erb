<%= form_for @job do |f| %>
	<p><%= f.label :name %>
	<%= f.text_field :name %></p>
	<p><%= f.label :pay_rate %>
	<%= f.number_field :pay_rate, :class => 'text_field', :step => 0.01 %></p>
	<p><%= f.label :description %>
	<%= f.text_area :description %></p>
	<%= f.submit :Submit %>
<% end %>